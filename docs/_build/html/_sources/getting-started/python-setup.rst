.. |python_link| raw:: html

   <a href="https://www.python.org/downloads/" target="_blank">Python.org</a>

Python setup
=====================

.. important::

    Installing multiple versions of Python on a system can raise several errors.
    If you already have Python installed, skip this section.
    You can check whether Python is installed by typing ``python`` or ``python3`` into a
    terminal. Some systems may have Python installed by default.

A Python 3+ installation is required to run NeuroFlow.

Downloading Python
-----------------------

Start by downloading the official installer for your system:

1. Navigate to the official Python release page: |python_link|.

2.
    Click the ``Download Python`` link.

    .. image:: static/python_download.png


Installing Python
-----------------------
    
Navigate to the downloaded Python installer and run it:

1. Ensure both the ``Use admin privileges`` and the ``Add python.exe to PATH`` checkboxes are selected.

2.
    Click ``Install Now``.

    .. image:: static/python_install_start.png

3.
    After the installation completes, click ``Disable path length limit`` before closing the installer.

    .. image:: static/python_install_end.png